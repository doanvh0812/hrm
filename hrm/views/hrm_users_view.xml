<odoo>
    <record id="view_hrm_users_form" model="ir.ui.view">
        <field name="name">res.user.form.view</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='access_rights']" position="inside">
                <group>
                    <group string="Quyền HRMS">
                        <field name="related" invisible="1"/>
                        <field name="block_id"/>
                        <field name="department_id" widget="many2many_tags" attrs="{'invisible': [('related', '!=', True)]}"/>
                        <field name="system_id" widget="many2many_tags" attrs="{'invisible': [('related', '=', True)]}"/>
                        <field name="company" widget="many2many_tags" attrs="{'invisible': [('related', '=', True)]}"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>
</odoo>